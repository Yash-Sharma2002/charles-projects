/**
 * Enum for InvitationStatus
 * @readonly
 */
enum InvitationStatus {
  Pending = "pending",
  Accepted = "accepted",
  Rejected = "rejected",
  Revoked = "revoked",
  Expired = "expired",
}

export default InvitationStatus;
